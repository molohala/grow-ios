import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeInterface(
    target: .{{ name }},
    targets: [
        .makeInterface(target: .{{ name }}, dependencies: [])
    ]
)
