import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeService(
    target: .{{ name }}Service,
    targets: [
        .service(target: .{{ name }}Service, dependencies: [
            .service(target: .BaseService)
        ])
    ]
)
