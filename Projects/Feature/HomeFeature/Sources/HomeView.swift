import SwiftUI
import DesignSystem

public struct HomeView: View {
    
    public init() {}
    
    public var body: some View {
        ScrollView {
            VStack {
                Text("갱")
            }
        }
        .background()
    }
}
